# 프로토타입 코어는 표준 라이브러리만으로 동작한다 (--sample 및 DoA RSS).
# 아래는 실제 리테일러 확장 시 권장 패키지.

# 정적/서버렌더 사이트 크롤링·스케줄링
scrapy>=2.11
# 또는 crawlee (Node 기반) — JS 프로젝트로 갈 경우

# JS 렌더 SPA (명품 편집숍 등) 헤드리스 렌더링
playwright>=1.44
# crawl4ai>=0.3     # LLM 기반 구조화 추출 대안

# LLM 기반 스키마 추출(신규 사이트 온보딩 가속)
# firecrawl-py>=1.0

# 가격 이력 저장 (SQLite는 표준 라이브러리, 확장 시)
# sqlalchemy>=2.0
